## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Build:
   ```
   npm run build
   npm run tauri build
   ```
3. Tauri outputs installers/executables in:
   ```
   src-tauri/target/release/
   ```
